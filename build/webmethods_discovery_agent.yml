central:
  agentName:
  organizationID:
  environment:

log:
  level: debug
  format: json
  output: stdout
  path: logs

webmethods:
  pollInterval: 30s
  url: ""
  maturityState: "${}"
  filter: "${}"
  auth: 
    username: "${}"
    password: "${}"